{
  "name": "roampro-ai-sidebar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=extension.js --format=iife --platform=browser --target=es2020",
    "dev": "esbuild src/index.ts --bundle --outfile=extension.js --format=iife --platform=browser --target=es2020 --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  }
}
